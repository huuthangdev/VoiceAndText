<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_voice"
    android:padding="15dp"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/btn_voicetotext"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginBottom="10dp"
        android:background="@drawable/custom"
        android:padding="20dp"
        android:text="TÌM KIẾM BẰNG GIỌNG NÓI"
        android:textColor="@android:color/white"
        android:textSize="15sp"
        tools:ignore="HardcodedText" />

    <Button
        android:id="@+id/btn_texttovoice"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btn_voicetotext"
        android:layout_centerInParent="true"
        android:background="@drawable/custom"
        android:padding="20dp"
        android:text="CHUYỂN GIỌNG NÓI THÀNH VĂN BẢN"
        android:textColor="@android:color/white"
        android:textSize="15sp"
        tools:ignore="HardcodedText" />

</RelativeLayout>